const { colors } = require('tailwindcss/defaultTheme');

module.exports = {
  purge: [],
  theme: {
    color: {
      green: colors.green
    },
    extend: {},
  },
  variants: {},
  plugins: [],
}
